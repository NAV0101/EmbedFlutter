rootProject.name = "EmbedFlutter"
include ':app'
setBinding(new Binding([gradle: this]))                                // new
evaluate(new File(                                                     // new
        settingsDir.parentFile,                                              // new
        'android_module/.android/include_flutter.groovy'                         // new
))
